filebeat.config:
#  inputs:
#    # Mounted `filebeat-inputs` configmap:
#    path: /usr/share/filebeat/inputs.d/*.yml
#    # Reload inputs configs as they change:
#    reload.enabled: true
  modules:
    path: /usr/share/filebeat/modules.d/*.yml
#    path: /Users/kan/own/github/beats/filebeat/modules.d/*.yml
    # Reload module configs as they change:
    reload.enabled: true

output.rainhub:
  hosts: ["127.0.0.1:10010"]
  batch_size: 20480
  retry_limit: 5
  timeout: 2
